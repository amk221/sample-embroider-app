{{page-title "SampleEmbroiderApp"}}

<FooSelect
  @value={{this.foo1}}
  @onSelect={{this.handleSelectFoo}} as |sb|
>
  {{#each (array (hash name='Foo') (hash name='Bar')) as |foo|}}
    <sb.Option @value={{foo}}>{{foo.name}}</sb.Option>
  {{/each}}
</FooSelect>

{{outlet}}